// Auto-generated by pult-codegen — do not edit manually.
// Run: cargo run -p pult-codegen -- generate

//! The show, reached by name instead of by string.
//!
//! `data::cues().nth(3).fade_in_ms().set(4000)` writes exactly what
//! `host::set(&["cues", "3", "fade_in_ms"], &json!(4000))` writes — the wire is
//! the same generic path-plus-JSON it has always been, and this is the compiler
//! checking the spelling. Which is also the failure mode: a plugin built against a
//! newer schema than the station it is running on compiles fine and gets a
//! per-path error at the one call that names something the station has not got.
//!
//! Generated from the `EntityMeta` and `CommandRegistration` inventories.
//! Introspection is still the way to ask what *this* station has, including the
//! collections this build never heard of.

use std::collections::HashMap;

use uuid::Uuid;

use crate::data::{Collection, Entity, Field, Singleton};
use crate::schema::*;

// ── What a command is given ──────────────────────────────────────────────────

/// Arguments for `sequences.goNext`.
///
/// Every field is optional, so `&Default::default()` is a whole call.
#[derive(Debug, Clone, Default, serde::Serialize)]
#[serde(rename_all = "camelCase")]
pub struct SequenceGoNextArgs {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub at: Option<f64>,
}

/// Arguments for `sequences.goToCue`.
#[derive(Debug, Clone, Default, serde::Serialize)]
#[serde(rename_all = "camelCase")]
pub struct SequenceGoToCueArgs {
    pub cue_id: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub at: Option<f64>,
}

/// Arguments for `sequences.off`.
///
/// Every field is optional, so `&Default::default()` is a whole call.
#[derive(Debug, Clone, Default, serde::Serialize)]
#[serde(rename_all = "camelCase")]
pub struct SequenceOffArgs {
    #[serde(skip_serializing_if = "Option::is_none")]
    pub at: Option<f64>,
}

// ── cues ────────────────────────────────────────────────────────────────

/// A single lighting state snapshot with timing information.
///
/// The `cues` collection.
pub fn cues() -> CueCollection {
    CueCollection { at: Collection::at("cues") }
}

/// The `cues` collection, reached by [`cues()`].
pub struct CueCollection {
    at: Collection,
}

impl CueCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<Cue>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> CueEntity {
        CueEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> CueEntity {
        CueEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &Cue) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `cues` row, reached by [`CueCollection::by_id`] or
/// [`CueCollection::nth`].
pub struct CueEntity {
    at: Entity,
}

impl CueEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<Cue, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &Cue) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// Fractional cue number (1.0, 1.5, 2.0) — allows insertions.
    ///
    /// PERSISTED.
    pub fn number(&self) -> Field<f64> {
        self.at.field("number")
    }

    /// PERSISTED.
    pub fn captures(&self) -> Field<Vec<ParameterCapture>> {
        self.at.field("captures")
    }

    /// PERSISTED.
    pub fn follow_mode(&self) -> Field<FollowMode> {
        self.at.field("follow_mode")
    }

    /// What every capture of this cue takes on the way up, unless it says its own.
    ///
    /// PERSISTED.
    pub fn fade_in_ms(&self) -> Field<u32> {
        self.at.field("fade_in_ms")
    }

    /// And on the way down. Zero is not "snap": it means this cue does not split its
    /// fade, and everything takes the in time in both directions.
    ///
    /// PERSISTED.
    pub fn fade_out_ms(&self) -> Field<u32> {
        self.at.field("fade_out_ms")
    }

    /// What shape this cue's captures fade on, unless one of them says its own.
    /// `None` is the show's default for each parameter's group, which is what every
    /// cue nobody has opened this control on means.
    ///
    /// One curve rather than one per direction, where the times are one each. A
    /// split *time* is what a designer asks for constantly — a look that builds
    /// slowly and snaps away — and a curve that eased on the way up and ran linear
    /// on the way down is a distinction nobody has asked for, so it stays one until
    /// somebody does.
    ///
    /// PERSISTED.
    pub fn easing(&self) -> Field<Option<Easing>> {
        self.at.field("easing")
    }

    /// True when this cue is currently being executed (output is active).
    ///
    /// SYNCED.
    pub fn is_active(&self) -> Field<bool> {
        self.at.field("is_active")
    }
}

// ── fixtures ────────────────────────────────────────────────────────────

/// A patched fixture instance — a specific unit in the rig.
///
/// The `fixtures` collection.
pub fn fixtures() -> FixtureCollection {
    FixtureCollection { at: Collection::at("fixtures") }
}

/// The `fixtures` collection, reached by [`fixtures()`].
pub struct FixtureCollection {
    at: Collection,
}

impl FixtureCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<Fixture>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> FixtureEntity {
        FixtureEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> FixtureEntity {
        FixtureEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &Fixture) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `fixtures` row, reached by [`FixtureCollection::by_id`] or
/// [`FixtureCollection::nth`].
pub struct FixtureEntity {
    at: Entity,
}

impl FixtureEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<Fixture, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &Fixture) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn fixture_type_id(&self) -> Field<Uuid> {
        self.at.field("fixture_type_id")
    }

    /// PERSISTED.
    pub fn address(&self) -> Field<FixtureAddress> {
        self.at.field("address")
    }

    /// Where this fixture is in the rig, **relative to whatever it hangs off**. None
    /// until it has been placed.
    ///
    /// A transform rather than a point and a direction, because a drawing places
    /// things: a light on a truss turns when the truss does, and a mirrored truss is
    /// a reflection no rotation can express. [`crate::types::scene::world_transform`]
    /// composes the chain.
    ///
    /// PERSISTED.
    pub fn position(&self) -> Field<Option<Transform>> {
        self.at.field("position")
    }

    /// The object it hangs off — a truss, or a group of them.
    ///
    /// PERSISTED.
    pub fn parent(&self) -> Field<Option<Uuid>> {
        self.at.field("parent")
    }

    /// Which layer of the drawing it belongs to.
    ///
    /// PERSISTED.
    pub fn layer(&self) -> Field<Option<Uuid>> {
        self.at.field("layer")
    }

    /// The class it is tagged with: "house rig", "touring".
    ///
    /// PERSISTED.
    pub fn class(&self) -> Field<Option<Uuid>> {
        self.at.field("class")
    }

    /// The focus point it is aimed at, where a drawing said one.
    ///
    /// PERSISTED.
    pub fn focus(&self) -> Field<Option<Uuid>> {
        self.at.field("focus")
    }

    /// The number on its own label — what an operator calls it, which is not its id
    /// and need not be unique. MVR's `FixtureID`.
    ///
    /// PERSISTED.
    pub fn fixture_number(&self) -> Field<Option<u32>> {
        self.at.field("fixture_number")
    }

    /// Which of several identical units this is, where a drawing numbered them.
    ///
    /// PERSISTED.
    pub fn unit_number(&self) -> Field<Option<u32>> {
        self.at.field("unit_number")
    }

    /// What this fixture's devices have *told* the console, keyed by parameter key:
    /// a contact closure, a temperature, a humidity — anything a device reports.
    ///
    /// The one kind of value the console still stores, and the reason it does is that
    /// it cannot work it out. What a fixture is being *driven* to is a function of the
    /// fades and effects on it, the programmer over them and its home value beneath,
    /// evaluated for whatever moment a consumer asks about; what it is *sensing*
    /// arrived off a wire and is not a function of anything the console holds.
    ///
    /// SYNCED, and for a reason that survived the removal of its driven half: nothing
    /// else on the network can work this value out, because the wire it came off is
    /// attached to this station.
    ///
    /// Defaulted on the wire so a showfile or a peer from before the split — where one
    /// map carried both halves under the name `live_values` — opens with this field
    /// simply absent. The old name is *ignored* rather than aliased onto this one: what
    /// it mostly carried was driven values, and those are now a function of what is
    /// driving them. Taking them for readings would be filing the console's own output
    /// as something a device reported.
    ///
    /// SYNCED.
    pub fn sensed_values(&self) -> Field<HashMap<String, ParameterValue>> {
        self.at.field("sensed_values")
    }

    /// What shape is driving each of this fixture's parameters, keyed by parameter
    /// key.
    ///
    /// Together with `live_fades` this is *what the fixture is doing*, and the whole of
    /// it: nothing stores the values these come to. A shape and its anchor are enough
    /// for anybody holding the row to work out what the parameter is at any moment they
    /// care about, which is what lets an output connector run at its protocol's rate, a
    /// node trace the shape itself, and a browser draw it at its own refresh.
    ///
    /// LOCAL rather than SYNCED. Every station works these out for itself from
    /// replicated cue state, so broadcasting them would be sending each console a
    /// slower copy of what it has already computed.
    ///
    /// Defaulted on the wire: a LOCAL field is worked out by the station that holds
    /// it, so a client creating a fixture has nothing to say about it and should not
    /// have to name it. Without this, `fixtures/__create` refuses a body that is
    /// otherwise complete because it omitted a field only the engine can fill.
    ///
    /// LOCAL.
    pub fn live_effects(&self) -> Field<HashMap<String, RunningEffect>> {
        self.at.field("live_effects")
    }

    /// The fades on each of this fixture's parameters, keyed by parameter key. LOCAL
    /// and defaulted for the same reasons as above.
    ///
    /// **Including the ones that have arrived**, which is what makes this the record of
    /// where the rig is rather than a list of what is in flight. A landed fade is a
    /// constant function of time, and since nothing stores the number it landed on, it
    /// is the only thing that remembers it. One entry per parameter anything has ever
    /// driven, replaced when a cue, a release or an action takes the key.
    ///
    /// LOCAL.
    pub fn live_fades(&self) -> Field<HashMap<String, RunningFade>> {
        self.at.field("live_fades")
    }

    /// What this fixture's parameters rest at when nothing is driving them, keyed by
    /// parameter key. Empty on nearly every fixture, and then the answer is
    /// whatever its type declares.
    ///
    /// On the fixture rather than on the type because a house light that comes up
    /// when nothing is controlling it is a fact about *this* rig, and because a type
    /// is derived: the node describes its ports again and the console rebuilds it,
    /// which would take an override with it. Defaulted on the wire so a show written
    /// before this existed opens with every fixture saying nothing.
    ///
    /// PERSISTED.
    pub fn home_values(&self) -> Field<HashMap<String, ParameterValue>> {
        self.at.field("home_values")
    }
}

// ── fixture_types ───────────────────────────────────────────────────────

/// Template describing what parameters a fixture type has.
///
/// The parameter list is what the light can *do*; [`FixtureType::modes`] is how those
/// parameters reach a DMX line, and which one a given unit uses is on its address.
///
/// `Default` is a blank type — no id, no name, no channels, and
/// [`FixtureTypeSource::Manual`], which is what a type nobody has filled in yet
/// honestly is. It is what the fields added since the patch went in are filled from,
/// so a caller that names the five it cares about does not have to name the rest.
///
/// The `fixture_types` collection.
pub fn fixture_types() -> FixtureTypeCollection {
    FixtureTypeCollection { at: Collection::at("fixture_types") }
}

/// The `fixture_types` collection, reached by [`fixture_types()`].
pub struct FixtureTypeCollection {
    at: Collection,
}

impl FixtureTypeCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<FixtureType>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> FixtureTypeEntity {
        FixtureTypeEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> FixtureTypeEntity {
        FixtureTypeEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &FixtureType) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `fixture_types` row, reached by [`FixtureTypeCollection::by_id`] or
/// [`FixtureTypeCollection::nth`].
pub struct FixtureTypeEntity {
    at: Entity,
}

impl FixtureTypeEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<FixtureType, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &FixtureType) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn manufacturer(&self) -> Field<String> {
        self.at.field("manufacturer")
    }

    /// The four-or-so characters a patch sheet has room for.
    ///
    /// PERSISTED.
    pub fn short_name(&self) -> Field<String> {
        self.at.field("short_name")
    }

    /// PERSISTED.
    pub fn long_name(&self) -> Field<String> {
        self.at.field("long_name")
    }

    /// PERSISTED.
    pub fn description(&self) -> Field<String> {
        self.at.field("description")
    }

    /// How many channels the default mode's first break occupies.
    ///
    /// Kept as a plain number because it is what the patch panel has always shown and
    /// what an operator means by "how big is it". The real answer, for a fixture with
    /// more than one break or more than one mode, is [`FixtureType::footprint`]; this
    /// is maintained alongside it rather than replaced, so nothing that read it had to
    /// change.
    ///
    /// PERSISTED.
    pub fn channel_count(&self) -> Field<u16> {
        self.at.field("channel_count")
    }

    /// PERSISTED.
    pub fn parameters(&self) -> Field<Vec<ParameterDefinition>> {
        self.at.field("parameters")
    }

    /// The ways this type can be addressed over DMX.
    ///
    /// Empty on everything the console made for itself, and then there is still one
    /// mode — see [`FixtureType::mode`], which computes it rather than a load-time
    /// rewrite writing it. Defaulted on the wire so a showfile from before modes
    /// existed opens with this field simply absent.
    ///
    /// PERSISTED.
    pub fn dmx_modes(&self) -> Field<Vec<DmxMode>> {
        self.at.field("dmx_modes")
    }

    /// What it weighs and draws.
    ///
    /// PERSISTED.
    pub fn physical(&self) -> Field<FixturePhysical> {
        self.at.field("physical")
    }

    /// The parts it is made of, for a 3D body that articulates.
    ///
    /// PERSISTED.
    pub fn geometry(&self) -> Field<Vec<FixtureGeometry>> {
        self.at.field("geometry")
    }

    /// Where this type came from, and so whether the console may rewrite it.
    ///
    /// PERSISTED.
    pub fn source(&self) -> Field<FixtureTypeSource> {
        self.at.field("source")
    }
}

// ── flows ───────────────────────────────────────────────────────────────

/// One graph.
///
/// The `flows` collection.
pub fn flows() -> FlowCollection {
    FlowCollection { at: Collection::at("flows") }
}

/// The `flows` collection, reached by [`flows()`].
pub struct FlowCollection {
    at: Collection,
}

impl FlowCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<Flow>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> FlowEntity {
        FlowEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> FlowEntity {
        FlowEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &Flow) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `flows` row, reached by [`FlowCollection::by_id`] or
/// [`FlowCollection::nth`].
pub struct FlowEntity {
    at: Entity,
}

impl FlowEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<Flow, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &Flow) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn enabled(&self) -> Field<bool> {
        self.at.field("enabled")
    }
}

// ── flow_edges ──────────────────────────────────────────────────────────

/// One connection, from an output port to an input port.
///
/// The `flow_edges` collection.
pub fn flow_edges() -> FlowEdgeCollection {
    FlowEdgeCollection { at: Collection::at("flow_edges") }
}

/// The `flow_edges` collection, reached by [`flow_edges()`].
pub struct FlowEdgeCollection {
    at: Collection,
}

impl FlowEdgeCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<FlowEdge>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> FlowEdgeEntity {
        FlowEdgeEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> FlowEdgeEntity {
        FlowEdgeEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &FlowEdge) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `flow_edges` row, reached by [`FlowEdgeCollection::by_id`] or
/// [`FlowEdgeCollection::nth`].
pub struct FlowEdgeEntity {
    at: Entity,
}

impl FlowEdgeEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<FlowEdge, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &FlowEdge) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn flow_id(&self) -> Field<Uuid> {
        self.at.field("flow_id")
    }

    /// PERSISTED.
    pub fn from_node(&self) -> Field<Uuid> {
        self.at.field("from_node")
    }

    /// Which output handle of `from_node`, indexing [`FlowNodeKind::outputs`].
    ///
    /// PERSISTED.
    pub fn from_port(&self) -> Field<u8> {
        self.at.field("from_port")
    }

    /// PERSISTED.
    pub fn to_node(&self) -> Field<Uuid> {
        self.at.field("to_node")
    }

    /// Which input handle of `to_node`, indexing [`FlowNodeKind::inputs`].
    ///
    /// PERSISTED.
    pub fn to_port(&self) -> Field<u8> {
        self.at.field("to_port")
    }
}

// ── flow_nodes ──────────────────────────────────────────────────────────

/// One node in one graph.
///
/// Nodes and edges are collections of their own rather than two `Vec`s on `Flow`,
/// so dragging a node patches one row instead of rewriting the graph, and two
/// operators moving two different nodes both keep their work.
///
/// The `flow_nodes` collection.
pub fn flow_nodes() -> FlowNodeCollection {
    FlowNodeCollection { at: Collection::at("flow_nodes") }
}

/// The `flow_nodes` collection, reached by [`flow_nodes()`].
pub struct FlowNodeCollection {
    at: Collection,
}

impl FlowNodeCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<FlowNode>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> FlowNodeEntity {
        FlowNodeEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> FlowNodeEntity {
        FlowNodeEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &FlowNode) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `flow_nodes` row, reached by [`FlowNodeCollection::by_id`] or
/// [`FlowNodeCollection::nth`].
pub struct FlowNodeEntity {
    at: Entity,
}

impl FlowNodeEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<FlowNode, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &FlowNode) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn flow_id(&self) -> Field<Uuid> {
        self.at.field("flow_id")
    }

    /// PERSISTED.
    pub fn kind(&self) -> Field<FlowNodeKind> {
        self.at.field("kind")
    }

    /// Where the node sits on the canvas. Not a rig position — this is the drawing.
    ///
    /// PERSISTED.
    pub fn x(&self) -> Field<f32> {
        self.at.field("x")
    }

    /// PERSISTED.
    pub fn y(&self) -> Field<f32> {
        self.at.field("y")
    }

    /// Lit right now: a level that is true, or a pulse that just passed through.
    ///
    /// SYNCED rather than LOCAL so every console watching the same flow sees the
    /// same thing light up. Without it a graph is a diagram; with it, it is an
    /// instrument you can watch working.
    ///
    /// SYNCED.
    pub fn active(&self) -> Field<bool> {
        self.at.field("active")
    }

    /// SYNCED.
    pub fn last_fired_at(&self) -> Field<Option<Timestamp>> {
        self.at.field("last_fired_at")
    }

    /// Press a button node.
    ///
    /// The press is this timestamp changing, and nothing else. That makes it an
    /// ordinary replicated field write, so a button pressed on a tablet reaches the
    /// leader — the only node that fires anything — by the path every other change
    /// already takes.
    ///
    /// The command `flow_nodes.press`, which the station applies to the row and
    /// replicates like any other write.
    pub fn press(&self) -> Result<(), String> {
        self.at.command("press", &serde_json::json!({}))
    }
}

// ── groups ──────────────────────────────────────────────────────────────

/// A saved selection: a name and the question it stands for.
///
/// The `groups` collection.
pub fn groups() -> GroupCollection {
    GroupCollection { at: Collection::at("groups") }
}

/// The `groups` collection, reached by [`groups()`].
pub struct GroupCollection {
    at: Collection,
}

impl GroupCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<Group>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> GroupEntity {
        GroupEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> GroupEntity {
        GroupEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &Group) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `groups` row, reached by [`GroupCollection::by_id`] or
/// [`GroupCollection::nth`].
pub struct GroupEntity {
    at: Entity,
}

impl GroupEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<Group, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &Group) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// What the group asks of the rig. Not the fixtures it currently picks out —
    /// those are worked out on demand, which is the whole point.
    ///
    /// PERSISTED.
    pub fn query(&self) -> Field<SelectionQuery> {
        self.at.field("query")
    }
}

// ── layers ──────────────────────────────────────────────────────────────

/// A drawing's layer: a name to show, hide and lock a part of the rig by.
///
/// Whether *this browser* is showing it is a Svelte store, not a field — two people
/// looking at one show should be able to look at different parts of it. Whether it is
/// locked is the show's, because that is a decision about the rig rather than about a
/// screen.
///
/// The `layers` collection.
pub fn layers() -> LayerCollection {
    LayerCollection { at: Collection::at("layers") }
}

/// The `layers` collection, reached by [`layers()`].
pub struct LayerCollection {
    at: Collection,
}

impl LayerCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<Layer>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> LayerEntity {
        LayerEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> LayerEntity {
        LayerEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &Layer) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `layers` row, reached by [`LayerCollection::by_id`] or
/// [`LayerCollection::nth`].
pub struct LayerEntity {
    at: Entity,
}

impl LayerEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<Layer, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &Layer) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn locked(&self) -> Field<bool> {
        self.at.field("locked")
    }

    /// Where it sits in the layers panel.
    ///
    /// Not `order`, which is a SQL keyword and which the generated `CREATE TABLE`
    /// does not quote — a column called that fails to open the show.
    ///
    /// PERSISTED.
    pub fn sort_order(&self) -> Field<u32> {
        self.at.field("sort_order")
    }
}

// ── layouts ─────────────────────────────────────────────────────────────

/// One saved arrangement of the workspace.
///
/// The `layouts` collection.
pub fn layouts() -> LayoutCollection {
    LayoutCollection { at: Collection::at("layouts") }
}

/// The `layouts` collection, reached by [`layouts()`].
pub struct LayoutCollection {
    at: Collection,
}

impl LayoutCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<Layout>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> LayoutEntity {
        LayoutEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> LayoutEntity {
        LayoutEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &Layout) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `layouts` row, reached by [`LayoutCollection::by_id`] or
/// [`LayoutCollection::nth`].
pub struct LayoutEntity {
    at: Entity,
}

impl LayoutEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<Layout, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &Layout) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// The whole arrangement, as one JSON column.
    ///
    /// PERSISTED.
    pub fn tree(&self) -> Field<LayoutNode> {
        self.at.field("tree")
    }
}

// ── named_assets ────────────────────────────────────────────────────────

/// The name a file gave an asset, against the sha it is stored under.
///
/// The asset store is content-addressed and has no names in it, and a mesh does have
/// names in it: a `.3ds` asks for `tx603.jpg` by that string and nothing else. This is
/// the bridge, in both directions — the browser resolves a texture name through it,
/// and an export writes each asset back under a name its own meshes still find.
///
/// The `named_assets` collection.
pub fn named_assets() -> NamedAssetCollection {
    NamedAssetCollection { at: Collection::at("named_assets") }
}

/// The `named_assets` collection, reached by [`named_assets()`].
pub struct NamedAssetCollection {
    at: Collection,
}

impl NamedAssetCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<NamedAsset>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> NamedAssetEntity {
        NamedAssetEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> NamedAssetEntity {
        NamedAssetEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &NamedAsset) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `named_assets` row, reached by [`NamedAssetCollection::by_id`] or
/// [`NamedAssetCollection::nth`].
pub struct NamedAssetEntity {
    at: Entity,
}

impl NamedAssetEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<NamedAsset, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &NamedAsset) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// UUIDv5 of the name, so two imports of one archive write one row.
    ///
    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// The name as the file spelled it.
    ///
    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// The sha256 it is stored under.
    ///
    /// PERSISTED.
    pub fn asset(&self) -> Field<String> {
        self.at.field("asset")
    }

    /// What kind of file it is, as it was stored.
    ///
    /// PERSISTED.
    pub fn mime(&self) -> Field<String> {
        self.at.field("mime")
    }
}

// ── outputs ─────────────────────────────────────────────────────────────

/// One configured output.
///
/// The `outputs` collection.
pub fn outputs() -> OutputConfigCollection {
    OutputConfigCollection { at: Collection::at("outputs") }
}

/// The `outputs` collection, reached by [`outputs()`].
pub struct OutputConfigCollection {
    at: Collection,
}

impl OutputConfigCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<OutputConfig>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> OutputConfigEntity {
        OutputConfigEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> OutputConfigEntity {
        OutputConfigEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &OutputConfig) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `outputs` row, reached by [`OutputConfigCollection::by_id`] or
/// [`OutputConfigCollection::nth`].
pub struct OutputConfigEntity {
    at: Entity,
}

impl OutputConfigEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<OutputConfig, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &OutputConfig) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn kind(&self) -> Field<OutputKind> {
        self.at.field("kind")
    }

    /// Where to send, as `host` or `host:port`. Required for Art-Net; for sACN an
    /// address means unicast to a receiver that multicast cannot reach.
    ///
    /// PERSISTED.
    pub fn target(&self) -> Field<Option<String>> {
        self.at.field("target")
    }

    /// Which universes to send. Empty means every universe in the patch.
    ///
    /// A routing rather than a label: the connector renders only these, so two
    /// outputs can split a rig between two interfaces and each evaluates its own
    /// half. Obeyed by every kind, an OpenHaunt output included — the sACN it feeds
    /// its gateway nodes is a universe on a wire like any other.
    ///
    /// PERSISTED.
    pub fn universes(&self) -> Field<Vec<u16>> {
        self.at.field("universes")
    }

    /// PERSISTED.
    pub fn enabled(&self) -> Field<bool> {
        self.at.field("enabled")
    }

    /// Which station sends this. `None` means every one of them, which puts the same
    /// frames on the wire once per node — useful on purpose for a redundant path,
    /// and a surprise by accident, so the UI fills in the local station.
    ///
    /// PERSISTED.
    pub fn node_id(&self) -> Field<Option<NodeId>> {
        self.at.field("node_id")
    }
}

// ── plugin_data ─────────────────────────────────────────────────────────

/// One thing a plugin remembers, in a store that travels with the show.
///
/// An ordinary entity on purpose. Being one buys SQLite persistence, replication
/// to peers, catch-up from the oplog, the snapshot round trip and vector-clock
/// conflict resolution with no code written for any of them — the promise task 2
/// made, taken. A bespoke table written by the host would have needed its own
/// replication, and a plugin's macros not reaching the second console would be
/// reported as "the plugin is broken".
///
/// **`id` is derived, not fresh.** It is a UUIDv5 over
/// `(plugin_id, store, key)`, so a key names the same row on every station. With
/// a random id, two stations each writing `macros/opening` would create two rows
/// holding one key — not a conflict the vector clock resolves, but a duplicate
/// it has no reason to notice, and a plugin reading back two values for one key.
/// [`PluginDatum::id_for`] is where that is spelled.
///
/// Station-scoped stores are deliberately *not* here: they are persistent and
/// not replicated, which is a combination the lifecycle enum has no name for,
/// and they live in a SQLite file beside the station's preferences instead.
///
/// The `plugin_data` collection.
pub fn plugin_data() -> PluginDatumCollection {
    PluginDatumCollection { at: Collection::at("plugin_data") }
}

/// The `plugin_data` collection, reached by [`plugin_data()`].
pub struct PluginDatumCollection {
    at: Collection,
}

impl PluginDatumCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<PluginDatum>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> PluginDatumEntity {
        PluginDatumEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> PluginDatumEntity {
        PluginDatumEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &PluginDatum) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `plugin_data` row, reached by [`PluginDatumCollection::by_id`] or
/// [`PluginDatumCollection::nth`].
pub struct PluginDatumEntity {
    at: Entity,
}

impl PluginDatumEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<PluginDatum, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &PluginDatum) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// Whose data this is. Survives the plugin being removed, which is what
    /// makes a mistaken removal recoverable and lets an operator see what is
    /// left behind and whose it was.
    ///
    /// PERSISTED.
    pub fn plugin_id(&self) -> Field<String> {
        self.at.field("plugin_id")
    }

    /// Which of that plugin's declared stores.
    ///
    /// PERSISTED.
    pub fn store(&self) -> Field<String> {
        self.at.field("store")
    }

    /// PERSISTED.
    pub fn key(&self) -> Field<String> {
        self.at.field("key")
    }

    /// PERSISTED.
    pub fn value(&self) -> Field<serde_json::Value> {
        self.at.field("value")
    }
}

// ── plugin_packages ─────────────────────────────────────────────────────

/// One plugin the show carries.
///
/// The bundle itself is not here: `sha256` names bytes in the asset store, which
/// is what lets a station that has never seen this plugin fetch it from a peer
/// and verify what came back. Two shows carrying the same plugin name the same
/// digest and share one copy.
///
/// The `plugin_packages` collection.
pub fn plugin_packages() -> PluginPackageCollection {
    PluginPackageCollection { at: Collection::at("plugin_packages") }
}

/// The `plugin_packages` collection, reached by [`plugin_packages()`].
pub struct PluginPackageCollection {
    at: Collection,
}

impl PluginPackageCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<PluginPackage>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> PluginPackageEntity {
        PluginPackageEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> PluginPackageEntity {
        PluginPackageEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &PluginPackage) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `plugin_packages` row, reached by [`PluginPackageCollection::by_id`] or
/// [`PluginPackageCollection::nth`].
pub struct PluginPackageEntity {
    at: Entity,
}

impl PluginPackageEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<PluginPackage, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &PluginPackage) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// The id from the bundle's own manifest. Unique across the roster, enforced
    /// at the install path rather than by the schema: nothing in the entity
    /// machinery expresses a unique secondary key, and inventing one here would
    /// be the first exception to a rule that has held since task 2.
    ///
    /// PERSISTED.
    pub fn plugin_id(&self) -> Field<String> {
        self.at.field("plugin_id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn version(&self) -> Field<String> {
        self.at.field("version")
    }

    /// The plugin API version the bundle was built against, copied from its
    /// manifest so the panel can explain a refusal without opening the zip.
    ///
    /// PERSISTED.
    pub fn api(&self) -> Field<String> {
        self.at.field("api")
    }

    /// The bundle's content address: the sha256 of the zip, in hex.
    ///
    /// PERSISTED.
    pub fn sha256(&self) -> Field<String> {
        self.at.field("sha256")
    }

    /// PERSISTED.
    pub fn enabled(&self) -> Field<bool> {
        self.at.field("enabled")
    }

    /// PERSISTED.
    pub fn stage(&self) -> Field<PluginStage> {
        self.at.field("stage")
    }

    /// Show-level configuration, merged over the manifest's own defaults and
    /// under this station's overrides. Never a home for a credential: it is in
    /// the showfile, so it replicates and it lands in every backup.
    ///
    /// PERSISTED.
    pub fn config(&self) -> Field<serde_json::Value> {
        self.at.field("config")
    }
}

// ── programmer_values ───────────────────────────────────────────────────

/// One parameter of one fixture, held by the programmer.
///
/// The `programmer_values` collection.
pub fn programmer_values() -> ProgrammerValueCollection {
    ProgrammerValueCollection { at: Collection::at("programmer_values") }
}

/// The `programmer_values` collection, reached by [`programmer_values()`].
pub struct ProgrammerValueCollection {
    at: Collection,
}

impl ProgrammerValueCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<ProgrammerValue>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> ProgrammerValueEntity {
        ProgrammerValueEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> ProgrammerValueEntity {
        ProgrammerValueEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &ProgrammerValue) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `programmer_values` row, reached by [`ProgrammerValueCollection::by_id`] or
/// [`ProgrammerValueCollection::nth`].
pub struct ProgrammerValueEntity {
    at: Entity,
}

impl ProgrammerValueEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<ProgrammerValue, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &ProgrammerValue) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Derived from `fixture_id` and the parameter key rather than minted, so two
    /// consoles writing the same parameter converge on one entry.
    ///
    /// SYNCED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// SYNCED.
    pub fn fixture_id(&self) -> Field<Uuid> {
        self.at.field("fixture_id")
    }

    /// SYNCED.
    pub fn parameter_kind(&self) -> Field<ParameterKind> {
        self.at.field("parameter_kind")
    }

    /// SYNCED.
    pub fn value(&self) -> Field<ParameterValue> {
        self.at.field("value")
    }

    /// A shape held instead of a value.
    ///
    /// An entry asserts either its value or its effect for the key, never both, so
    /// the id derivation is unchanged: grabbing a fader and putting a sine on it are
    /// the same act of taking hold of one parameter.
    ///
    /// SYNCED.
    pub fn effect(&self) -> Field<Option<EffectSpec>> {
        self.at.field("effect")
    }

    /// Parked: survives Clear and Store, so one value can go into several cues.
    ///
    /// The spec calls this the parking function and asks for it explicitly — a value
    /// held "without saving, to be saved in multiple sequences without the need of a
    /// store menu".
    ///
    /// SYNCED.
    pub fn locked(&self) -> Field<bool> {
        self.at.field("locked")
    }
}

// ── classes ─────────────────────────────────────────────────────────────

/// A tag that cuts across layers: "house rig", "touring", "practicals".
///
/// MVR's `Class`, kept rather than dropped for two reasons. It is data the file
/// carries that nothing else in the show could reconstruct, and an export that lost it
/// would not round-trip — every real file in the corpus classes every object it has.
///
/// The `classes` collection.
pub fn classes() -> SceneClassCollection {
    SceneClassCollection { at: Collection::at("classes") }
}

/// The `classes` collection, reached by [`classes()`].
pub struct SceneClassCollection {
    at: Collection,
}

impl SceneClassCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<SceneClass>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> SceneClassEntity {
        SceneClassEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> SceneClassEntity {
        SceneClassEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &SceneClass) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `classes` row, reached by [`SceneClassCollection::by_id`] or
/// [`SceneClassCollection::nth`].
pub struct SceneClassEntity {
    at: Entity,
}

impl SceneClassEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<SceneClass, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &SceneClass) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }
}

// ── scene_objects ───────────────────────────────────────────────────────

/// A truss, a rostrum, a screen: something in the rig that is not a light.
///
/// The `scene_objects` collection.
pub fn scene_objects() -> SceneObjectCollection {
    SceneObjectCollection { at: Collection::at("scene_objects") }
}

/// The `scene_objects` collection, reached by [`scene_objects()`].
pub struct SceneObjectCollection {
    at: Collection,
}

impl SceneObjectCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<SceneObject>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> SceneObjectEntity {
        SceneObjectEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> SceneObjectEntity {
        SceneObjectEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &SceneObject) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `scene_objects` row, reached by [`SceneObjectCollection::by_id`] or
/// [`SceneObjectCollection::nth`].
pub struct SceneObjectEntity {
    at: Entity,
}

impl SceneObjectEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<SceneObject, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &SceneObject) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// The MVR uuid where it came from one, so a re-import matches rather than
    /// duplicates.
    ///
    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn kind(&self) -> Field<SceneObjectKind> {
        self.at.field("kind")
    }

    /// Where it is **relative to its parent**. Composing the chain is
    /// [`world_transform`].
    ///
    /// PERSISTED.
    pub fn transform(&self) -> Field<Transform> {
        self.at.field("transform")
    }

    /// The object this hangs off, if any.
    ///
    /// PERSISTED.
    pub fn parent(&self) -> Field<Option<Uuid>> {
        self.at.field("parent")
    }

    /// PERSISTED.
    pub fn layer(&self) -> Field<Option<Uuid>> {
        self.at.field("layer")
    }

    /// PERSISTED.
    pub fn class(&self) -> Field<Option<Uuid>> {
        self.at.field("class")
    }

    /// Meshes of its own.
    ///
    /// PERSISTED.
    pub fn geometry(&self) -> Field<Vec<GeometryRef>> {
        self.at.field("geometry")
    }

    /// Or a shared one, which is how a rig of forty identical truss sections holds
    /// one mesh rather than forty.
    ///
    /// PERSISTED.
    pub fn symbol(&self) -> Field<Option<Uuid>> {
        self.at.field("symbol")
    }

    /// Or none of the above: a piece out of the console's own catalogue, named
    /// rather than modelled — `"f34-2m"` is two metres of box truss and the browser
    /// draws one.
    ///
    /// Last of the three, and a mesh that came with a drawing wins: it is the truth
    /// about that object. This is what a console draws for a rig it made itself,
    /// where until now there was nothing to draw and the lights hung in the air.
    ///
    /// See [`crate::types::catalogue`].
    ///
    /// PERSISTED.
    pub fn catalogue(&self) -> Field<Option<String>> {
        self.at.field("catalogue")
    }
}

// ── sequences ───────────────────────────────────────────────────────────

/// An ordered list of cues (called a "sequence" by the operator).
///
/// The `sequences` collection.
pub fn sequences() -> SequenceCollection {
    SequenceCollection { at: Collection::at("sequences") }
}

/// The `sequences` collection, reached by [`sequences()`].
pub struct SequenceCollection {
    at: Collection,
}

impl SequenceCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<Sequence>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> SequenceEntity {
        SequenceEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> SequenceEntity {
        SequenceEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &Sequence) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `sequences` row, reached by [`SequenceCollection::by_id`] or
/// [`SequenceCollection::nth`].
pub struct SequenceEntity {
    at: Entity,
}

impl SequenceEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<Sequence, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &Sequence) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// Ordered list of cue IDs belonging to this sequence.
    ///
    /// PERSISTED.
    pub fn cue_ids(&self) -> Field<Vec<Uuid>> {
        self.at.field("cue_ids")
    }

    /// Index of the currently active cue within cue_ids, if any.
    ///
    /// SYNCED.
    pub fn active_cue_index(&self) -> Field<Option<usize>> {
        self.at.field("active_cue_index")
    }

    /// Console unix ms the current cue went.
    ///
    /// Fades and effects are both measured from here, which is why it is replicated
    /// rather than kept per station: two consoles rendering the same cue have to
    /// agree on when it started, not merely that it did.
    ///
    /// SYNCED.
    pub fn went_at(&self) -> Field<Option<u64>> {
        self.at.field("went_at")
    }

    /// Advance to the next cue, staying on the last one when there is no next.
    ///
    /// Running out of cues is not turning the sequence off. A light does not go dark
    /// because the operator pressed Go once more than there were cues, and the way to
    /// say that is to leave the last cue owning what it faded to — rather than to
    /// leave the values behind under a cue marked inactive, which is what wrapping to
    /// `None` used to do. Having only [`Sequence::off`] reach `None` is also what
    /// makes "nothing is driving these parameters any more" a thing playback can see.
    ///
    /// Args: `{ "at": <console unix ms> }`, optional. Every station runs this command
    /// from the same arguments, so carrying the time is what makes all of them anchor
    /// the cue at the same millisecond rather than at whenever each of them got here.
    ///
    /// The command `sequences.goNext`, which the station applies to the row and
    /// replicates like any other write.
    pub fn go_next(&self, args: &SequenceGoNextArgs) -> Result<(), String> {
        self.at.command("goNext", args)
    }

    /// Jump to the cue with the given ID. Args: { "cueId": "<uuid>", "at"?: <ms> }
    ///
    /// The command `sequences.goToCue`, which the station applies to the row and
    /// replicates like any other write.
    pub fn go_to_cue(&self, args: &SequenceGoToCueArgs) -> Result<(), String> {
        self.at.command("goToCue", args)
    }

    /// Take the sequence off: no cue of it is active any more.
    ///
    /// The act this console did not have. Everything it was driving and nothing else
    /// is still driving goes to its home value, which playback works out for itself
    /// from the cues — so this command writes one field and every station in the
    /// session arrives at the same rig.
    ///
    /// Carries its time like a Go, so the release fades from the same millisecond
    /// everywhere.
    ///
    /// The command `sequences.off`, which the station applies to the row and
    /// replicates like any other write.
    pub fn off(&self, args: &SequenceOffArgs) -> Result<(), String> {
        self.at.command("off", args)
    }
}

// ── show ────────────────────────────────────────────────────────────────

/// Top-level show metadata.
///
/// The `show` singleton.
pub fn show() -> ShowSingleton {
    ShowSingleton { at: Singleton::at("show") }
}

/// The `show` singleton, reached by [`show()`].
pub struct ShowSingleton {
    at: Singleton,
}

impl ShowSingleton {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole of it, or `None` where the show has not got one yet.
    pub fn get(&self) -> Result<Option<Show>, String> {
        self.at.get()
    }

    /// Replace the whole of it.
    pub fn set(&self, value: &Show) -> Result<(), String> {
        self.at.set(value)
    }

    /// Be told when it changes; updates arrive at `PultPlugin::on_update`.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn created_at(&self) -> Field<Timestamp> {
        self.at.field("created_at")
    }

    /// The cue currently being edited, if any.
    ///
    /// Editing is load-tweak-Update rather than live: the cue is read into the
    /// programmer, changed there, and written back on Update. This says which cue is
    /// waiting for that write, and it is SYNCED so a second console shows the same
    /// banner rather than quietly storing over the first one's work.
    ///
    /// SYNCED.
    pub fn editing_cue(&self) -> Field<Option<Uuid>> {
        self.at.field("editing_cue")
    }

    /// How many of an operator's own changes stay reachable, for undo and for the
    /// history panel.
    ///
    /// Changes, not presses. An undo is a change too and shares the window with the
    /// ones it reverses, so a run of them meets itself somewhere around half way:
    /// five hundred is on the order of two hundred and fifty pressings of Ctrl-Z in
    /// a row, and every one of them a long way past where anybody is still sure what
    /// they are undoing.
    ///
    /// Show data rather than a station setting, so that two consoles working one
    /// show agree about how far back Ctrl-Z goes. A station's own preference decides
    /// what a *new* show starts with and then stops mattering — a default that kept
    /// applying would let two stations give different answers about the same show,
    /// which for undo is not a preference but a disagreement.
    ///
    /// PERSISTED.
    pub fn history_depth(&self) -> Field<u32> {
        self.at.field("history_depth")
    }

    /// How long a parameter takes to reach its home value when nothing is left
    /// driving it — a sequence taken off, a selection sent home.
    ///
    /// Show data for the same reason `history_depth` is, and the reason is easier to
    /// see here: two stations driving one rig and fading it home over different
    /// times is not a preference but a disagreement the audience can watch. A
    /// station's own preference decides what a *new* show starts with.
    ///
    /// Zero, so nothing an operator is used to changes until they ask for it: a
    /// programmer clear has always snapped.
    ///
    /// PERSISTED.
    pub fn home_fade_ms(&self) -> Field<u32> {
        self.at.field("home_fade_ms")
    }

    /// How much haze the rig view draws the beams through, 0 to 1.
    ///
    /// How much haze is in the room, 0 to 1. At 0 the air is clear and no beam is
    /// drawn — the light still lands on the floor — and at 1 every beam shows with the
    /// folds haze puts in it.
    ///
    /// Show data rather than a per-browser view setting, and this one was argued
    /// both ways. How hazy the room is is a fact about the room: a designer who has
    /// lit a piece for a hazy house wants the next person opening the file to see
    /// what they saw, and two operators disagreeing about it are disagreeing about
    /// the drawing rather than about their own screens. A station preference decides
    /// what a *new* show starts with, the way `home_fade_ms` does.
    ///
    /// It reaches no lamp. Nothing here changes what leaves the console.
    ///
    /// PERSISTED.
    pub fn haze_density(&self) -> Field<f32> {
        self.at.field("haze_density")
    }

    /// How much the haze moves, 0 to 1. Separate from density because a still,
    /// heavy haze and a thin, churning one are different rooms, and one number
    /// cannot say which.
    ///
    /// PERSISTED.
    pub fn haze_turbulence(&self) -> Field<f32> {
        self.at.field("haze_turbulence")
    }

    /// What shape a fade has when neither the capture nor the cue says one.
    ///
    /// Show data for the reason `home_fade_ms` is, and the reason is the same one
    /// again: two stations running one cue with the heads easing on one desk and
    /// running linear on the other is not a preference but a disagreement the
    /// audience can watch. A station's own preference decides what a *new* show
    /// starts with.
    ///
    /// PERSISTED.
    pub fn fade_curves(&self) -> Field<FadeCurves> {
        self.at.field("fade_curves")
    }
}

// ── speed_masters ───────────────────────────────────────────────────────

/// One tempo several effects can follow.
///
/// The `speed_masters` collection.
pub fn speed_masters() -> SpeedMasterCollection {
    SpeedMasterCollection { at: Collection::at("speed_masters") }
}

/// The `speed_masters` collection, reached by [`speed_masters()`].
pub struct SpeedMasterCollection {
    at: Collection,
}

impl SpeedMasterCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<SpeedMaster>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> SpeedMasterEntity {
        SpeedMasterEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> SpeedMasterEntity {
        SpeedMasterEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &SpeedMaster) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `speed_masters` row, reached by [`SpeedMasterCollection::by_id`] or
/// [`SpeedMasterCollection::nth`].
pub struct SpeedMasterEntity {
    at: Entity,
}

impl SpeedMasterEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<SpeedMaster, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &SpeedMaster) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn bpm(&self) -> Field<f32> {
        self.at.field("bpm")
    }

    /// Halve or double without losing the tapped tempo underneath.
    ///
    /// PERSISTED.
    pub fn multiplier(&self) -> Field<f32> {
        self.at.field("multiplier")
    }

    /// SYNCED.
    pub fn running(&self) -> Field<bool> {
        self.at.field("running")
    }

    /// Console unix ms of the last "one". Rewritten by every tap and every bpm edit.
    ///
    /// SYNCED.
    pub fn t0(&self) -> Field<u64> {
        self.at.field("t0")
    }
}

// ── stage_plans ─────────────────────────────────────────────────────────

/// A drawing of the room, laid under the rig.
///
/// The `stage_plans` collection.
pub fn stage_plans() -> StagePlanCollection {
    StagePlanCollection { at: Collection::at("stage_plans") }
}

/// The `stage_plans` collection, reached by [`stage_plans()`].
pub struct StagePlanCollection {
    at: Collection,
}

impl StagePlanCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<StagePlan>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> StagePlanEntity {
        StagePlanEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> StagePlanEntity {
        StagePlanEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &StagePlan) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `stage_plans` row, reached by [`StagePlanCollection::by_id`] or
/// [`StagePlanCollection::nth`].
pub struct StagePlanEntity {
    at: Entity,
}

impl StagePlanEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<StagePlan, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &StagePlan) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// The image's sha256 in the asset store.
    ///
    /// PERSISTED.
    pub fn asset(&self) -> Field<String> {
        self.at.field("asset")
    }

    /// The image's own size, so the view can lay it out before the bytes arrive.
    ///
    /// PERSISTED.
    pub fn width_px(&self) -> Field<u32> {
        self.at.field("width_px")
    }

    /// PERSISTED.
    pub fn height_px(&self) -> Field<u32> {
        self.at.field("height_px")
    }

    /// The world point, in metres, that the image's top-left corner sits on.
    ///
    /// PERSISTED.
    pub fn origin(&self) -> Field<Vec3> {
        self.at.field("origin")
    }

    /// How much of the room one image pixel covers. Set by measuring something on
    /// the plan whose real length is known.
    ///
    /// PERSISTED.
    pub fn metres_per_pixel(&self) -> Field<f32> {
        self.at.field("metres_per_pixel")
    }

    /// PERSISTED.
    pub fn rotation_deg(&self) -> Field<f32> {
        self.at.field("rotation_deg")
    }

    /// PERSISTED.
    pub fn opacity(&self) -> Field<f32> {
        self.at.field("opacity")
    }

    /// PERSISTED.
    pub fn visible(&self) -> Field<bool> {
        self.at.field("visible")
    }
}

// ── stations ────────────────────────────────────────────────────────────

/// One console taking part in the session.
///
/// The `stations` collection.
pub fn stations() -> StationCollection {
    StationCollection { at: Collection::at("stations") }
}

/// The `stations` collection, reached by [`stations()`].
pub struct StationCollection {
    at: Collection,
}

impl StationCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<Station>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> StationEntity {
        StationEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> StationEntity {
        StationEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &Station) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `stations` row, reached by [`StationCollection::by_id`] or
/// [`StationCollection::nth`].
pub struct StationEntity {
    at: Entity,
}

impl StationEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<Station, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &Station) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// The station's `NodeId`, so the row is stable across restarts and every node
    /// writes to the same key for the same machine.
    ///
    /// SYNCED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// SYNCED.
    pub fn hostname(&self) -> Field<String> {
        self.at.field("hostname")
    }

    /// SYNCED.
    pub fn is_leader(&self) -> Field<bool> {
        self.at.field("is_leader")
    }

    /// Where peers reach this station, as `ip:port`.
    ///
    /// SYNCED.
    pub fn sync_addr(&self) -> Field<String> {
        self.at.field("sync_addr")
    }

    /// Where this station serves its HTTP API, as `ip:port`.
    ///
    /// Assets are bytes rather than replicated fields, so a station that has never
    /// seen a stage plan fetches it from one that has — and this is the address it
    /// fetches from. Defaulted, because an older peer will not send it.
    ///
    /// SYNCED.
    pub fn http_addr(&self) -> Field<String> {
        self.at.field("http_addr")
    }

    /// SYNCED.
    pub fn cpu_percent(&self) -> Field<f32> {
        self.at.field("cpu_percent")
    }

    /// SYNCED.
    pub fn mem_used(&self) -> Field<u64> {
        self.at.field("mem_used")
    }

    /// SYNCED.
    pub fn mem_total(&self) -> Field<u64> {
        self.at.field("mem_total")
    }

    /// How long this backend has been up, not the machine.
    ///
    /// SYNCED.
    pub fn uptime_s(&self) -> Field<u64> {
        self.at.field("uptime_s")
    }

    /// The outputs this station is sending, by name.
    ///
    /// SYNCED.
    pub fn output_plugins(&self) -> Field<Vec<String>> {
        self.at.field("output_plugins")
    }

    /// How many fixtures this station computes, and how many exist.
    ///
    /// Equal on every station today: playback runs everywhere, which is what makes
    /// output deterministic without extra messages. They come apart when parameter
    /// computation is partitioned, and the pair is here so that when it happens the
    /// UI already says something true.
    ///
    /// SYNCED.
    pub fn computes_fixtures(&self) -> Field<u32> {
        self.at.field("computes_fixtures")
    }

    /// SYNCED.
    pub fn total_fixtures(&self) -> Field<u32> {
        self.at.field("total_fixtures")
    }

    /// What each of this station's output connectors' frames cost over the window
    /// just reported, one entry per connector.
    ///
    /// Empty when nothing emitted a frame at all in that window — a station with no
    /// output configured, or one whose protocols are all idle — because zero would
    /// read as "instant", which is the opposite of what happened. One entry per
    /// connector rather than one figure for the station, because their rates and
    /// their costs are their own: Art-Net drawing at 40 Hz beside an OpenHaunt node
    /// that was told about a fade once are not two samples of one number.
    ///
    /// Defaulted, so a peer running a build that cannot say still sends a row that
    /// deserialises.
    ///
    /// SYNCED.
    pub fn frame_costs(&self) -> Field<Vec<FrameCost>> {
        self.at.field("frame_costs")
    }

    /// What the *machine* is doing, as against what this console is costing.
    ///
    /// Every other figure on this row is the console's own — `cpu_percent` and
    /// `mem_used` are this process and deliberately so, because a console sharing a
    /// box with something else should report what it is costing rather than what the
    /// box is. These are the other half of that sentence, and the pair is the point:
    /// a station at 4% on a machine at 96% is not a comfortable station, it is a
    /// console about to be starved by something nobody is looking at.
    ///
    /// All defaulted, so a peer on an older build still deserialises.
    ///
    /// SYNCED.
    pub fn machine(&self) -> Field<MachineStats> {
        self.at.field("machine")
    }

    /// What this machine's network interfaces carried over the window just reported,
    /// each way, and how long that window was.
    ///
    /// **The whole machine, not this console.** Every other figure in this row is what
    /// the console is responsible for; this is what the cable is actually carrying,
    /// which includes whatever else the box is doing. The two are worth having side by
    /// side and must never be confused: a station whose own output accounts for 2 MB/s
    /// on an interface carrying 90 MB/s has a network problem that is not its fault,
    /// and one where the two figures agree has found its own.
    ///
    /// Loopback is excluded. On a laptop running `demo.sh` every byte this console
    /// sends to itself would otherwise arrive twice — sent and received — and swamp
    /// the figure it is there to give.
    ///
    /// Defaulted, so a peer on an older build still deserialises.
    ///
    /// SYNCED.
    pub fn net_received(&self) -> Field<u64> {
        self.at.field("net_received")
    }

    /// SYNCED.
    pub fn net_sent(&self) -> Field<u64> {
        self.at.field("net_sent")
    }

    /// SYNCED.
    pub fn net_window_ms(&self) -> Field<u32> {
        self.at.field("net_window_ms")
    }

    /// When this station last said any of the above.
    ///
    /// SYNCED.
    pub fn last_seen(&self) -> Field<Timestamp> {
        self.at.field("last_seen")
    }
}

// ── symbols ─────────────────────────────────────────────────────────────

/// A piece of geometry drawn once and placed many times.
///
/// The `symbols` collection.
pub fn symbols() -> SymbolCollection {
    SymbolCollection { at: Collection::at("symbols") }
}

/// The `symbols` collection, reached by [`symbols()`].
pub struct SymbolCollection {
    at: Collection,
}

impl SymbolCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<Symbol>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> SymbolEntity {
        SymbolEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> SymbolEntity {
        SymbolEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &Symbol) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `symbols` row, reached by [`SymbolCollection::by_id`] or
/// [`SymbolCollection::nth`].
pub struct SymbolEntity {
    at: Entity,
}

impl SymbolEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<Symbol, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &Symbol) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn geometry(&self) -> Field<Vec<GeometryRef>> {
        self.at.field("geometry")
    }
}

// ── users ───────────────────────────────────────────────────────────────

/// One person working on the show.
///
/// The `users` collection.
pub fn users() -> UserCollection {
    UserCollection { at: Collection::at("users") }
}

/// The `users` collection, reached by [`users()`].
pub struct UserCollection {
    at: Collection,
}

impl UserCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<User>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> UserEntity {
        UserEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> UserEntity {
        UserEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &User) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `users` row, reached by [`UserCollection::by_id`] or
/// [`UserCollection::nth`].
pub struct UserEntity {
    at: Entity,
}

impl UserEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<User, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &User) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// PERSISTED.
    pub fn name(&self) -> Field<String> {
        self.at.field("name")
    }

    /// How this user's changes are marked in the history, as a CSS colour.
    ///
    /// Persisted rather than derived from the id, so somebody who dislikes the one
    /// they were given can change it and it stays changed.
    ///
    /// PERSISTED.
    pub fn colour(&self) -> Field<String> {
        self.at.field("colour")
    }
}

// ── versions ────────────────────────────────────────────────────────────

/// One saved point in a show's history.
///
/// The `versions` collection.
pub fn versions() -> VersionCollection {
    VersionCollection { at: Collection::at("versions") }
}

/// The `versions` collection, reached by [`versions()`].
pub struct VersionCollection {
    at: Collection,
}

impl VersionCollection {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// Every row, in the show's own order.
    pub fn get(&self) -> Result<Vec<Version>, String> {
        self.at.get()
    }

    /// One row by its id.
    pub fn by_id(&self, id: Uuid) -> VersionEntity {
        VersionEntity { at: self.at.by_id(id) }
    }

    /// One row by position in the collection's order.
    pub fn nth(&self, index: usize) -> VersionEntity {
        VersionEntity { at: self.at.nth(index) }
    }

    /// Add a row. One gesture, so it is one Ctrl-Z for whoever asked.
    pub fn create(&self, value: &Version) -> Result<(), String> {
        self.at.create(value)
    }

    /// Be told when the collection itself changes — a create, a delete.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe()
    }

    /// Be told about anything at or under the collection, a level moving
    /// included.
    pub fn subscribe_deep(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// Put something back where it rests when nothing is driving it.
    ///
    /// `programmer_values`. `{ "fixtureId": <uuid> }` sends every output parameter of
    /// that fixture home, and naming a `parameterKind` as well sends just the one. The
    /// station resolves it against what it holds, so a plugin can ask for home without
    /// being able to read the rig.
    pub fn home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__home", args)
    }

    /// Make where a parameter rests be wherever it is now.
    ///
    /// `fixtures`, and the same arguments as [`Self::home`] backwards. Evaluated at
    /// the instant it is asked, which is why it is a verb and not a write.
    pub fn take_home(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__set_home", args)
    }

    /// Save: a point to come back to.
    ///
    /// `versions`. `{ "name": "Act 1" }`, and a quick Save gives no name. A verb
    /// rather than a create because two of the row's fields are the engine's own.
    pub fn checkpoint(&self, args: &serde_json::Value) -> Result<(), String> {
        self.at.verb("__checkpoint", args)
    }
}

/// One `versions` row, reached by [`VersionCollection::by_id`] or
/// [`VersionCollection::nth`].
pub struct VersionEntity {
    at: Entity,
}

impl VersionEntity {
    /// The path this accessor writes, as the station spells it.
    pub fn path(&self) -> &[String] {
        self.at.path()
    }

    /// The whole row.
    pub fn get(&self) -> Result<Version, String> {
        self.at.get()
    }

    /// Replace the whole row.
    pub fn set(&self, value: &Version) -> Result<(), String> {
        self.at.set(value)
    }

    /// Delete the row.
    pub fn delete(&self) -> Result<(), String> {
        self.at.delete()
    }

    /// Be told when this row changes.
    pub fn subscribe(&self) -> u64 {
        self.at.subscribe_deep()
    }

    /// PERSISTED.
    pub fn id(&self) -> Field<Uuid> {
        self.at.field("id")
    }

    /// What the operator called it, where they called it anything. A quick Save has
    /// no name, and the panel shows the time instead — naming every checkpoint is
    /// work nobody does, and a console that demanded one would be a console nobody
    /// saved on.
    ///
    /// PERSISTED.
    pub fn name(&self) -> Field<Option<String>> {
        self.at.field("name")
    }

    /// PERSISTED.
    pub fn created_at(&self) -> Field<Timestamp> {
        self.at.field("created_at")
    }

    /// Who took it. `None` for one the console took by itself — an autosave, or the
    /// one taken before a restore.
    ///
    /// PERSISTED.
    pub fn user_id(&self) -> Field<Option<Uuid>> {
        self.at.field("user_id")
    }

    /// Whether the console took this one rather than a person.
    ///
    /// Kept apart because the two are pruned differently and read differently: an
    /// operator's saves are theirs to keep, and the automatic ones are a rolling
    /// window the console trims to `autosave_keep`. The panel dims them for the same
    /// reason it dims the console's own writes in the history.
    ///
    /// PERSISTED.
    pub fn automatic(&self) -> Field<bool> {
        self.at.field("automatic")
    }

    /// Where the show's clock stood when this was taken.
    ///
    /// Nothing reads it yet. It is here because a version is the only record of a
    /// past state that survives the oplog's retention, and a diff between two of
    /// them has to be able to say which came first on which station — which a
    /// timestamp across machines with unsynchronised clocks cannot.
    ///
    /// PERSISTED.
    pub fn clock(&self) -> Field<VectorClock> {
        self.at.field("clock")
    }
}

