{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The panel listens for the node's own events and calls its own commands. Nothing else.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
